// import { create } from 'zustand'

// interface StoreProp {
//     isLoading: boolean;
//     setLoading: (value: boolean) => void
// }

// export const useUiStore = create<StoreProp>((set) => ({
//     isLoading: false,
//     setLoading: (value) => set({ isLoading: value })
// }))